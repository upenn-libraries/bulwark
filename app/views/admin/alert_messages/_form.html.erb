<%= bootstrap_form_for [:admin, alert_message] do |f| %>
  <%= f.check_box :active %>
  <%= f.select :level,
               options_for_select(AlertMessage::LEVELS, alert_message.level),
               label: 'Level' %>
  <%= f.text_area :message %>
  <%= f.submit 'Save' %>
<% end %>
