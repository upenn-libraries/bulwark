
<p>Colenda is a system for long-term preservation and access to digital assets stewarded by <%= link_to(t('homepage.institution_link_text'), t('homepage.institution_link_href')) %>.  It is a project of the Libraries, developed using the Samvera software framework.  At this time, content accessible to the public in this application is in the <%= link_to(t('homepage.public_domain_link_text'), t('homepage.public_domain_link_href')) %> and is therefore available for public use, or where indicated, is of <%= link_to(t('homepage.undetermined_copyright_link_text'), t('homepage.undetermined_copyright_link_href')) %>.</p>

<% if @home_alert.active? %>
  <div class="alert alert-<%= @home_alert.level %>"><%= @home_alert.message %></div>
<% end %>

<%= t('homepage.featured_title_html') %>
<ul class="bxslider">
  <%= render_featured_list %>
</ul>

<script type="text/javascript">
    $(document).ready(function($) {
        var slider = $('.bxslider').bxSlider({
            captions: true,
            speed: 1000,
            ariaHidden: true,
            ariaLive: true,
            auto: true,
            autoHover: true,
            stopAutoOnClick: true,
            pause: 8000
        });
        // set keyboard listener
        $(document).keydown(function(e){
            if (e.keyCode == 39) // Right arrow
            {
                slider.goToNextSlide();
                return false;
            }
            else if (e.keyCode == 37) // left arrow
            {
                slider.goToPrevSlide();
                return false;
            }
        });
    });
</script>
